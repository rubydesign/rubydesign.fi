- if clerk_signed_in?
  %li= link_to 'Logout', destroy_clerk_session_path, :method=>'delete'
- else
  %li= link_to 'Login', new_clerk_session_path
- if clerk_signed_in?
  %li= link_to 'Edit account', edit_clerk_registration_path
- else
  %li= link_to 'Sign up', new_clerk_registration_path
- if clerk_signed_in?
  - if current_clerk.has_role? :admin
    %li= link_to 'Admin', clerks_path
