%h2
  = t(:treeview, :default => 'Treeview')
  = t('app.models.purchase.bs_caption', :default => 'purchase')
- namespace_for_url = ""
- plural_model_name = "purchases"
- model_name        = "purchase"
- opened_node       = Purchase.select(:id).all.map{ |g| "'treeelt_" + g.id.to_s + "'" }.join(',').html_safe
#treeview{"data-model" => model_name, "data-opened" => "[#{opened_node}]", "data-url" => "/#{namespace_for_url}#{plural_model_name}/"}
  %ul
    - Purchase.transaction do
      - ar = Purchase.where(:purchase_id => nil)
      - ar = ar.order("position") if Purchase.column_names.include?("position")
      - for g in ar.all
        = build_treeview(g, 'purchases')
