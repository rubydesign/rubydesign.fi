<h2><%= t(:new, :default => 'New') %> <%= t('app.models.order.bs_caption', :default => 'order') %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), orders_path, :class => "btn" %>
