<h2><%= t(:editing, :default => 'Editing') %> <%= t('app.models.order.bs_caption', :default => 'order') %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), orders_path, :class => "btn" %>