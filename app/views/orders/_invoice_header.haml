.row.header
  .col-6
    = image_tag "invoice-logo.gif",  :id => "logo"
    .sender
      = raw RubyClerks.config(:shop_address)
    %br
  .col-6.text-right
    %p.title= I18n.t("#{@template}.header")
    %b= I18n.t(:order_number).to_s + " " + @order.id_number
    &nbsp;/&nbsp;
    = I18n.l(@order.ordered_on.to_date) if @order.ordered_on
