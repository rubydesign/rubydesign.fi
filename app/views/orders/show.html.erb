<h2><%= t(:show, :default => "Show") %> <%= t('app.models.order.bs_caption', :default => 'order') %></h2>

  <p>
    <b><%= t('app.models.order.bs_attributes.ordered_on', :default => 'ordered_on') %>:</b>
      <%= @order.ordered_on %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.total_price', :default => 'total_price') %>:</b>
      <%= number_to_currency(@order.total_price) %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.total_tax', :default => 'total_tax') %>:</b>
      <%= number_to_currency(@order.total_tax) %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.shipping_price', :default => 'shipping_price') %>:</b>
      <%= number_to_currency(@order.shipping_price) %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.shipping_tax', :default => 'shipping_tax') %>:</b>
      <%= @order.shipping_tax %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.bucket', :default => 'bucket') %>:</b>
      <%= @order.bucket %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.email', :default => 'email') %>:</b>
      <%= @order.email %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.paid_on', :default => 'paid_on') %>:</b>
      <%= @order.paid_on %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.shipped_on', :default => 'shipped_on') %>:</b>
      <%= @order.shipped_on %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.paid_on', :default => 'paid_on') %>:</b>
      <%= @order.paid_on %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.canceled_on', :default => 'canceled_on') %>:</b>
      <%= @order.canceled_on %>
  </p>
  <p>
    <b><%= t('app.models.order.bs_attributes.shipment_type', :default => 'shipment_type') %>:</b>
      <%= @order.shipment_type %>
  </p>

<!-- Beautiful_scaffold - AddField - Field - Do not remove -->

<%= link_to t(:edit, :default => "Edit"), edit_order_path(@order), :class => "btn" %>
<%= link_to t(:back, :default => "Back"), orders_path, :class => "btn" %>

