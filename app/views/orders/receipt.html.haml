!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %title= t(:receipt)
    = stylesheet_link_tag "html-receipt"
    = stylesheet_link_tag "html-receipt" , :media => "print"
  %body{onload: "self.print()"}
    .receipt
      #header
        = image_tag "receipt-logo.gif"
        %br/
        %br/
        .shop
          Auringosta Itään, Kuusta Länteen
        .info
          Fredrikinkatu 19 (Viiskulma)
          %br/
          00120 Helsinki
      .info
        - date = @order.ordered_on || @order.created_at
        =l(date.to_date) 
        /
        =t(:order)
        =@order.number
        %br/
      .wrapper
        .line_items
          %table.line_items
            %tbody
              - @order.basket.items.each do |i|
                %tr
                  %td.cell= i.product.name
                  %td.cell.qty= i.quantity
                  %td.cell.total= euros i.total
        .spacer
           
        .totals
          %table.totals
            %tbody
              %tr
                %td.cel1.bold
                  %strong= t(:order_total)
                %td.cel2.cell{align: "right"}
                  %strong
                    %span#summary-order-total= euros @order.total_price
              %tr
                %td.cel1
                %td.cel2
              -@order.taxes.each do |tax_rate , amount|
                %tr.total
                  %td.cel1.bold
                    = t(:vat) + " " + tax_rate.to_s + " % "
                  %td.cel2.cell{align: "right"}= euros amount
      .footer
        %br/
        www.auringostaitaan.fi
        %br/
        info@www.auringostaitaan.fi
        %br/
        puh 09- 621 7001
