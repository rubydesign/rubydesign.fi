<h2><%= t(:editing, :default => 'Editing') %> <%= t('app.models.address.bs_caption', :default => 'address') %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), addresses_path, :class => "btn" %>