%h2
  = t(:show, :default => "Show")
  = t('app.models.basket.bs_caption', :default => 'basket')
%p
  %b
    = t('app.models.basket.bs_attributes.string', :default => 'string') + ":"
  = @basket.string
= link_to t(:edit), edit_basket_path(@basket), :class => "btn"
= link_to t(:back), baskets_path, :class => "btn"
