<h2><%= t(:editing, :default => 'Editing') %> <%= t('app.models.supplier.bs_caption', :default => 'supplier') %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), suppliers_path, :class => "btn" %>