<h2><%= t(:new, :default => 'New') %> <%= t('app.models.supplier.bs_caption', :default => 'supplier') %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), suppliers_path, :class => "btn" %>
