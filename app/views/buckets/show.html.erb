<h2><%= t(:show, :default => "Show") %> <%= t('app.models.bucket.bs_caption', :default => 'bucket') %></h2>

  <p>
    <b><%= t('app.models.bucket.bs_attributes.string', :default => 'string') %>:</b>
      <%= @bucket.string %>
  </p>

<!-- Beautiful_scaffold - AddField - Field - Do not remove -->

<%= link_to t(:edit, :default => "Edit"), edit_bucket_path(@bucket), :class => "btn" %>
<%= link_to t(:back, :default => "Back"), buckets_path, :class => "btn" %>

